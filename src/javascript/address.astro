<script client:load>
  document.addEventListener("DOMContentLoaded", async () => {
    const ipElementA = document.getElementById("user-ip-a");
    const ipElementB = document.getElementById("user-ip-b");
    if (!ipElementA && !ipElementB) return;

    ipElementA.textContent = `Đang lấy địa chỉ...A`;
    ipElementB.textContent = `Đang lấy địa chỉ...B`;

    try {
      // const response = await fetch("https://ipinfo.io/json?token=87aa8f284fb4f6");
      // const data = await response.json();
      ipElementA.textContent = 'Là cái gì...?' || `Địa chỉ: ${data.ip}`;
      ipElementB.textContent = 'Là cái chi...?' || `Địa chỉ: ${data.ip}`;
    } catch (error) {
      console.error("Lỗi khi lấy địa chỉ:", error);
      ipElement.textContent = `Không thể lấy địa chỉ.`;
    }
  });
</script>
